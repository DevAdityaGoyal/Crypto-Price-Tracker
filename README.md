<<<<<<< HEAD
# Crypto Price Tracker (Vanilla JS + PWA)

Fast, accessible, framework-free crypto dashboard with watchlist, currency switcher, dark/light theme, and installable PWA. Ships as static files — perfect for GitHub Pages, Netlify, or Vercel.

## ✨ Features

- **Live markets**: price, 1h/24h/7d %, market cap, volume, supply, 7d sparklines.
- **Search / Sort / Filter** (Top 10/50/100), **infinite scroll placeholder**.
- **Watchlist** (localStorage) and **deep linkable** detail modal (`?coin=bitcoin`).
- **Detail modal**: big header, FDV, ATH/ATL with dates, supply stats, and **interactive chart** (7d/30d/90d via Chart.js ESM, lazy-loaded).
- **Currency switcher**: USD / EUR / INR using `Intl.NumberFormat`.
- **Dark/Light theme**: OS autodetect + manual toggle (persisted).
- **PWA**: installable, offline shell, **CacheFirst** for static assets, **Stale-While-Revalidate** API cache (2 min TTL), offline banner.
- **A11y**: semantic table/cards, caption, `aria-sort`, focus rings, skip link, color + arrows for % change, keyboardable.
- **Performance**: lazy charts/sparklines (IntersectionObserver), async modules, < 90KB JS (excluding Chart.js), CSS < 40KB.
- **Security**: strict CSP (allow only self + api.coingecko.com + jsdelivr), no trackers/cookies, safe text updates.

## 🗂 Structure

=======
# Crypto Price Tracker (Vanilla JS + PWA)

Fast, accessible, framework-free crypto dashboard with watchlist, currency switcher, dark/light theme, and installable PWA. Ships as static files — perfect for GitHub Pages, Netlify, or Vercel.

## ✨ Features

- **Live markets**: price, 1h/24h/7d %, market cap, volume, supply, 7d sparklines.
- **Search / Sort / Filter** (Top 10/50/100), **infinite scroll placeholder**.
- **Watchlist** (localStorage) and **deep linkable** detail modal (`?coin=bitcoin`).
- **Detail modal**: big header, FDV, ATH/ATL with dates, supply stats, and **interactive chart** (7d/30d/90d via Chart.js ESM, lazy-loaded).
- **Currency switcher**: USD / EUR / INR using `Intl.NumberFormat`.
- **Dark/Light theme**: OS autodetect + manual toggle (persisted).
- **PWA**: installable, offline shell, **CacheFirst** for static assets, **Stale-While-Revalidate** API cache (2 min TTL), offline banner.
- **A11y**: semantic table/cards, caption, `aria-sort`, focus rings, skip link, color + arrows for % change, keyboardable.
- **Performance**: lazy charts/sparklines (IntersectionObserver), async modules, < 90KB JS (excluding Chart.js), CSS < 40KB.
- **Security**: strict CSP (allow only self + api.coingecko.com + jsdelivr), no trackers/cookies, safe text updates.

## 🗂 Structure

>>>>>>> 530db5daaec23920649897806d4a51bfac202cc5
